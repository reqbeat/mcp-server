# Claude Code

```bash
claude mcp add --transport http com-reqbeat-hiring-signals https://mcp.reqbeat.com/mcp
```

Add `--header "X-API-Key: <your key>"` for full access. Without it the server runs in demo mode.
